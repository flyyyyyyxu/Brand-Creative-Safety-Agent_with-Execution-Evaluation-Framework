# Python dependencies for Brand Safety Agent
# Install: pip3 install -r requirements.txt

# Core dependencies
playwright>=1.40.0
jsonschema>=4.17.0
python-dotenv>=1.0.0

# API clients
anthropic>=0.18.0  # For Claude API (if using as fallback)

# Optional: data processing
pandas>=2.0.0  # For advanced analytics
numpy>=1.24.0

# Optional: HTTP requests
requests>=2.31.0
aiohttp>=3.9.0  # Async HTTP

# Development dependencies
ipdb>=0.13.13  # Interactive debugger
pytest>=7.4.0  # Testing

# Note: OpenClaw installation depends on its distribution method
# If it's a private package, add installation instructions here